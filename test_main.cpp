#include <iostream>
#include "Komplexe_Zahlen_head.hpp"
#include <vector>
#include <array>
#include <cmath>
#include <fstream>

int main()
{
  /*
  KomplexND a({Komplex(1,1),Komplex(2,2),Komplex(3,3)});
  KomplexND b({Komplex(2,2),Komplex(4,4),Komplex(1,1)});
  KomplexND c = a + b;

  std::cout << c.atK(1) << std::endl;
  */
  Komplex a;
  std::cin >> a;
  std::cout << a << std::endl;


  return 0;
}
